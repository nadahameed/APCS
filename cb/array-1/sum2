public int sum2(int[] nums) {
  int sum = 0;
  if(nums.length < 2){
    if(nums.length == 0){
      sum = 0;
    }
    if(nums.length == 1){
      sum = nums[0];
    }
  } else {
  sum = nums[0] + nums[1];
  }
  return sum;
}
